apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "runbook.fullname" . }}-test-connection"
  labels:
    {{- include "runbook.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "runbook.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
